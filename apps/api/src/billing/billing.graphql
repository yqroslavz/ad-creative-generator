enum BillingTier {
  BASIC
  PRO
}

type CheckoutSession {
  url: String!
}

extend type Query {
  myBalance: Int!
}

extend type Mutation {
  createCheckoutSession(tier: BillingTier!): CheckoutSession!
}
